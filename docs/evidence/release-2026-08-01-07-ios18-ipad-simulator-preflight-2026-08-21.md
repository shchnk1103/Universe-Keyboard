# RELEASE-2026-0801-07 — iOS 18 iPad Simulator Preflight

> **Status:** Simulator matrix complete / release-toolchain runtime gate pending
> **Evidence grade:** Simulator compatibility and defect-discovery evidence only
> **Authority:** [`RELEASE-2026-0801-07`](../assignments/release-2026-08-01-07-ipad-support.md)

## Run Header

| Field | Frozen value |
|---|---|
| Date / timezone | `2026-08-21 Asia/Shanghai` |
| Authorization | Human Product Owner authorized the current Codex task to continue and delegated simple Simulator launch operations to the Human operator when useful |
| Executor | Current Codex task — Keyboard Experience execution thread |
| Environment operators | Codex for build/install/inspection; Human Product Owner for the explicitly requested Simulator boot and later subjective checks |
| Repository / branch | `Universe Keyboard` / `codex/external-testflight-cloud-prep` |
| Source state | Base `c339591`, plus the explicitly authorized candidate VoiceOver semantics remediation in the current working tree; the affected Extension was rebuilt and reinstalled before Human revalidation |
| Local toolchain | Xcode 27.0 beta 5 (`27A5237l`) on macOS 27 beta for iOS 18/26 Simulator compatibility execution; Xcode 26.6 (`17F113`) is installed and passes SwiftPM, but its iOS 26.5 Simulator runtime/platform component is not installed locally |
| Stable-toolchain reference | Xcode Cloud Build 1 on `cdc6bfe`: Xcode 26.6 (`17F113`), macOS Tahoe 26.6.2 (`25G83`), completed with no Issues |
| Runtime | iOS 18.0 (`22A3351`) |
| Scheme / configuration | `Universe Keyboard` / Debug for inspection; Release or final archive is not claimed |
| Privacy boundary | Synthetic input only; screenshots and logs must not contain real user input, account secrets or private content |
| Expiry | Product/UI/RIME behavior change, deployment-target change, Simulator runtime change, or final RC selection |

## Frozen Matrix

| Tier | Device | UDID | Coverage |
|---|---|---|---|
| Primary | iPad (10th generation) | `83BCF163-FE7D-4B5E-9654-AB71920CDD82` (actual booted instance) | Portrait/landscape; light/dark; default and accessibility Dynamic Type; basic VoiceOver; Main App and Keyboard Extension smoke |
| Small boundary | iPad mini (6th generation) | `FF9A7369-7A66-4DF1-A51B-E01E751967F2` (actual booted instance; supersedes the unused Shutdown inventory instance `D2381C21-…`) | Portrait/landscape; key Main App and Keyboard surfaces; clipping/geometry regression discovery |
| Large boundary | iPad Pro 13-inch (M4) (16GB) | `CBF6B78C-47B3-4071-B2DE-02F89180CC96` (actual booted instance; supersedes unused Shutdown 8GB inventory instance `1ABCAEE1-…`) | Portrait/landscape; key Main App and Keyboard surfaces; excessive-width/spacing regression discovery |

## Checks

| Area | Planned observation | Result |
|---|---|---|
| Build/install/launch | Debug build for exact source; install and launch on primary iOS 18 Simulator | Passed; delayed screenshot shows rendered Home and first-run welcome sheet |
| Main App | Home, onboarding/keyboard enablement, settings, diagnostics/help/search surfaces; no clipping or unreachable controls | Partial pass: Home, welcome/activation, Help, Settings/RIME/layout and Search critical surfaces were exercised across the matrix without observed clipping; Diagnostics breadth remains pending. |
| Keyboard Extension | Enable in Simulator, open a synthetic host field, basic Chinese/English/9-key switching and candidate/path chrome | Passed on primary critical smoke: Full Access-off 26-key input loop and Chinese/English recovery; verified fog T9 readiness; Full Access nine-key runtime, `64` composition/commit/deletion without raw digits; Chinese nine-key ↔ English 26-key round-trip; and landscape T9 input/commit. Boundary-device coverage remains separate. |
| Rotation | Main App and visible keyboard survive portrait/landscape transitions without overlap or stale geometry | Passed on primary for both layouts: 26-key portrait → landscape → portrait passed; nine-key portrait → landscape input/commit → portrait recovery preserved committed host text and idle composition state without stale geometry. |
| Appearance | Primary critical surfaces in light and dark mode | Passed on primary portrait critical path: coherent Main App/Extension surfaces, readable marked text and readable custom candidate treatments; iPadOS input-assistant rendering remains system-owned |
| Dynamic Type | Default plus one accessibility size; content remains reachable and labels do not silently truncate critical meaning | Passed on primary portrait critical path: AXXXL retains complete keyboard/candidates, wraps App text and keeps lower content reachable by scrolling |
| VoiceOver | Primary navigation/labels/order smoke; no claim of complete accessibility certification | Partial pass after remediation: the original candidate-role defect was reproduced, minimally fixed, rebuilt/reinstalled and Human-revalidated as “呢，按钮，双击选择候选词”. The adjacent expand control also announces its role and hint. Complete navigation/order coverage remains pending. |
| Boundary sizes | Repeat critical surfaces on mini and 13-inch devices | Passed at critical-smoke grade: actual iPad mini 6 and actual 13-inch M4 instances each covered first-run/activation plus Full Access-off portrait/landscape 26-key geometry, composition and candidate commit. |

## Automated Validation

| Gate | Result |
|---|---|
| Swift format | Passed: strict lint for `CandidateCell.swift` and `CandidateAccessibilityContractTests.swift` |
| KeyboardCore / Xcode 26.6 | Passed: `1030` tests, `0` failures |
| RimeBridge / Xcode 27 + iOS 26.0 | Passed: `68` tests executed, `20` environment-gated Spike tests skipped, `0` failures; result bundle `/private/tmp/universe-keyboard-gate-rimebridge/Logs/Test/Test-RimeBridgeTests-2026.08.22_12-15-12-+0800.xcresult` |
| Main App + Keyboard / Xcode 27 + iOS 18.0 | Passed: Main App `189` tests with `3` physical-device-only skips and `0` failures; Keyboard `9` tests with `0` failures, including the new candidate accessibility contract; result bundle `/private/tmp/universe-keyboard-gate-xcode27-ios18-tests/Logs/Test/Test-Universe Keyboard-2026.08.22_12-22-09-+0800.xcresult` |
| Debug / Release build, Xcode 27 + iOS 18.0 | Passed with Swift 6 complete concurrency and warnings-as-errors; isolated DerivedData under `/private/tmp/universe-keyboard-gate-xcode27-ios18-{debug,release}` |
| Exact local Xcode 26.6 iOS gates | Pending environment prerequisite: Xcode reports the iOS 26.5 Simulator platform/runtime is not installed, so it exposes no eligible iOS Simulator destination. This is not recorded as a source failure or a passed gate. |
| Xcode 27 + iOS 26.0 Main App test attempt | Environment-incompatible and interrupted: unrelated existing tests repeatedly crashed at the same allocator address while the runtime reported an incompatible IOHID plugin architecture. The iOS 18 run above passed the same suites; this beta-toolchain/runtime attempt is retained as a non-release environment finding. |
| Full Access deferral remediation supplement | Passed: strict formatting for 3 changed Swift files; focused activation suite `20/20`; KeyboardCore `1030/1030`; RimeBridge `68` executed with `20` environment-gated skips; complete iOS 18 Main App `191` with `3` physical-device-only skips and Keyboard `9`, all with `0` failures; RIME Vendor `12` artifacts; Debug and Release builds with Swift 6 complete concurrency and warnings-as-errors. |

## Explicit Non-claims

- Simulator does not prove physical-iPad touch behavior, performance, memory pressure, Jetsam, hardware keyboard behavior or real Full Access lifecycle.
- This run does not freeze an RC, validate signing/Archive/artifact retention, upload a build or authorize TestFlight/App Store actions.
- Any source modification invalidates the affected observations until rebuilt and repeated.

## Execution Log

| Time | Step | Result / evidence |
|---|---|---|
| `2026-08-21 Asia/Shanghai` | Inventory installed runtimes/devices | iOS 18.0 runtime available; all three frozen devices already exist and were Shutdown, so no new Simulator creation is required |
| `2026-08-21 Asia/Shanghai` | Primary boot handoff | Human Product Owner asked to boot the existing iPad (10th generation) / iOS 18.0 and stop at the Home Screen |
| `2026-08-21 Asia/Shanghai` | RIME vendor preflight | `bash scripts/ensure_rime_vendor.sh verify` passed the structural inventory of 12 framework artifacts |
| `2026-08-21 Asia/Shanghai` | Primary Debug build | `xcodebuild` targeting the inventoried iPad 10 / iOS 18 instance (`47C7B1FF-…`) with Swift 6 complete concurrency and warnings-as-errors succeeded; isolated DerivedData: `/private/tmp/universe-keyboard-ipad18-preflight`. The resulting arm64 iOS 18 Simulator bundle was installed on the actual same-model/runtime instance below. |
| `2026-08-21 Asia/Shanghai` | Built bundle inspection | App `com.DoubleShy0N.Universe-Keyboard` and embedded Extension `com.DoubleShy0N.Universe-Keyboard.Keyboard` both declare `MinimumOSVersion = 18.0`; both privacy manifests are present |
| `2026-08-21 Asia/Shanghai` | Actual boot binding | Human-booted device is the same iPad 10 / iOS 18.0 model/runtime but a different instance (`83BCF163-…`) from the inventory UDID. The first install attempt against the stale Shutdown UDID failed before mutation; Run Header was rebound to the actual device. |
| `2026-08-21 Asia/Shanghai` | Install and launch | App installed and launched as PID `23232`. An immediate screenshot caught the blank launch surface; a delayed screenshot showed the rendered Home screen and welcome sheet, so the blank frame is classified as capture-too-early, not a crash/hang. |
| `2026-08-21 Asia/Shanghai` | Keyboard enablement handoff | Human operator asked to follow the first-run system-settings path, add Universe Keyboard with Full Access off, then return to the App |
| `2026-08-21 Asia/Shanghai` | Keyboard registration | Human operator reported the keyboard added; Simulator `pluginkit` lists `com.DoubleShy0N.Universe-Keyboard.Keyboard (1.0)`. The App activation screen renders without clipping and reports shared data unavailable while Full Access is off, as expected. App-local “添加键盘” acknowledgement still requires the visible “我已添加，继续” action. |
| `2026-08-22 Asia/Shanghai` | Full Access-off progression audit | Human screenshot shows Full Access remains off and the guide says it may be enabled later, but exposes only “打开设置” and “我已开启，继续”. Source inspection confirms the latter writes `activation_full_access_affirmed = true` and clears the unavailable flag before re-observation; it is not a truthful skip action. The run therefore leaves it untouched and continues through the always-visible Search tab. This is a Product Polish/onboarding affordance residual owned by `RELEASE-2026-0801-06`, not a reason to fabricate completion. |
| `2026-08-22 Asia/Shanghai` | Extension visible with Full Access off | Human operator selected Universe Keyboard in the Search field and confirmed the extension is visible. Screenshot `/private/tmp/universe-keyboard-ipad18-full-access-off-keyboard-visible.png` shows the iPad portrait layout with all primary letter rows and function keys visible, with no observed clipping or overlap. This proves only extension presentation in the current Simulator state; input, candidate selection and deletion remain unverified. |
| `2026-08-22 Asia/Shanghai` | Pinyin composition with Full Access off | Human operator tapped the extension's on-screen keys for the fixed synthetic input `nihao`. Screenshot `/private/tmp/universe-keyboard-ipad18-nihao-composition.png` shows underlined marked text `nihao` in the host search field and visible candidates `你好 / 拟好 / 你号`, with no observed keyboard displacement, clipping or overlap. This verifies composition and local candidate presentation in this Simulator state; candidate commit remains unverified. |
| `2026-08-22 Asia/Shanghai` | Candidate commit with Full Access off | Human operator selected the first candidate `你好`. Screenshot `/private/tmp/universe-keyboard-ipad18-nihao-candidate-committed.png` shows committed text `你好` without the composition underline; the candidate area refreshed to post-commit suggestions and the keyboard remained visible and geometrically stable. Post-commit deletion remains unverified. |
| `2026-08-22 Asia/Shanghai` | Post-commit deletion with Full Access off | Human operator tapped backspace once after committing `你好`. Screenshot `/private/tmp/universe-keyboard-ipad18-post-commit-delete.png` shows the host text reduced to `你`, with no stale marked text or visible keyboard instability. The portrait Full Access-off path now has an observed input loop covering extension presentation, composition, candidate presentation, candidate commit and one-character deletion. |
| `2026-08-22 Asia/Shanghai` | Portrait-to-landscape transition | Human operator rotated the active iPad Simulator while the extension remained visible. Screenshot `/private/tmp/universe-keyboard-ipad18-landscape-keyboard.png` shows preserved host text `你`, reachable host content, all letter/function rows and bottom safe area, with no observed clipping, overlap or stale portrait geometry. A post-rotation touch/input check remains required before calling the transition passed. |
| `2026-08-22 Asia/Shanghai` | Post-rotation input and candidate recovery | Human operator tapped `hao` on the landscape on-screen keyboard. The first capture (`/private/tmp/universe-keyboard-ipad18-landscape-hao-composition.png`) showed the expected marked text but was temporarily covered by the iPadOS system Edit shortcut panel. After the Human dismissed that system panel without changing input, `/private/tmp/universe-keyboard-ipad18-landscape-hao-after-dismiss.png` shows preserved marked text `hao`, candidates `好 / 号 / 浩`, and the complete landscape keyboard. This verifies post-rotation touch mapping, composition retention and candidate presentation; the transient system panel is not classified as an App/Extension defect. |
| `2026-08-22 Asia/Shanghai` | Landscape candidate commit | Human operator selected landscape candidate `好`. Screenshot `/private/tmp/universe-keyboard-ipad18-landscape-hao-committed.png` shows committed host text `你好` without marked-text underline; the keyboard remains fully visible and stable. This verifies the landscape candidate hit target and commit path in the current Simulator state. |
| `2026-08-22 Asia/Shanghai` | Return-to-portrait transition | Human operator returned to portrait through the Simulator Device menu. The first capture showed preserved `你好` but was covered by the same iPadOS system Edit shortcut panel; after human dismissal, `/private/tmp/universe-keyboard-ipad18-returned-portrait-after-dismiss.png` shows the complete portrait keyboard, preserved text and no observed clipping, overlap or stale landscape geometry. The primary-device portrait → landscape → portrait transition is passed; no further Command rotation shortcuts will be used in this run. |
| `2026-08-22 Asia/Shanghai` | Dark appearance initial observation | Codex switched the Simulator through `simctl ui … appearance dark`, without sending keyboard events. Screenshot `/private/tmp/universe-keyboard-ipad18-dark-portrait.png` shows coherent dark surfaces for the Main App, search field, cards, keyboard tray, letter/function keys and preserved `你好`, with no observed clipping or light-surface flash. The post-commit toolbar text `吗` appears potentially low-contrast against the dark tray and is held as a visual residual pending a dark-mode candidate/input capture; Appearance is not yet called passed. |
| `2026-08-22 Asia/Shanghai` | Dark appearance candidate attribution and contrast | Human operator entered synthetic `ni`. Screenshot `/private/tmp/universe-keyboard-ipad18-dark-ni-composition.png` shows readable marked text and the Extension-owned 34pt candidate row `你 / 呢 / 尼`: preferred candidate uses a white pill with dark text and remaining candidates use light text. Source trace shows Universe Keyboard constructs only that compact candidate collection plus expand control (`candidateBarHeight = 34`); it does not construct undo/redo/clipboard controls. Apple's `UITextInputAssistantItem` contract identifies the separate upper iPad shortcuts bar as UIKit-managed, including center typing suggestions and text-management controls. The earlier low-contrast `吗` is therefore recorded as system input-assistant rendering in this Simulator state, not an Extension-owned contrast defect. |
| `2026-08-22 Asia/Shanghai` | Accessibility Dynamic Type — AXXXL | Codex set Simulator `content_size` to `accessibility-extra-extra-extra-large`. The immediate capture `/private/tmp/universe-keyboard-ipad18-dark-axxxl-ni.png` caught an incomplete keyboard during system reflow; a later no-input capture `/private/tmp/universe-keyboard-ipad18-dark-axxxl-ni-stable.png` shows the complete Extension keyboard and candidate row, so the first frame is classified as transient reflow rather than a persistent layout failure. App text scales and wraps without silent truncation in the visible region; lower card content extends behind the keyboard, so scroll reachability remains pending. |
| `2026-08-22 Asia/Shanghai` | AXXXL Main App scroll reachability | Human operator swiped the Main App content above the keyboard. Screenshot `/private/tmp/universe-keyboard-ipad18-dark-axxxl-after-scroll.png` shows the lower explanatory card content brought fully into view while the Extension keyboard and candidate row remain stable. The primary portrait AXXXL critical path is passed for this Simulator-grade run. |
| `2026-08-22 Asia/Shanghai` | VoiceOver setup and Simulator input model | Human operator enabled VoiceOver. The one-time iPadOS gesture tutorial was dismissed, and `/private/tmp/universe-keyboard-ipad18-voiceover-ready.png` shows focus on the host search field with the Extension visible. On macOS Simulator, pointer hover starts speech and a click places accessibility focus; this differs from direct-device touch exploration. A later screenshot (`/private/tmp/universe-keyboard-ipad18-voiceover-candidate-clicked.png`) confirms the click placed the black VoiceOver focus ring on candidate `你` without committing it: host marked text `ni` remained active. |
| `2026-08-22 Asia/Shanghai` | VoiceOver candidate semantics — defect | Human operator hovered compact candidate `呢` and reported that VoiceOver spoke only `呢`, with no role or action hint. As a control, hovering the adjacent expand button spoke “展开更多候选词，按钮” and a double-tap-to-view-more-candidates hint. Source trace confirmed `CandidateCollectionCell` set candidate label/hint but did not explicitly declare `.button` traits, while the expand control is a `UIButton`. This reproducible production accessibility residual was handed to `RELEASE-2026-0801-06`, and the Human Product Owner explicitly authorized the minimal remediation. |
| `2026-08-22 Asia/Shanghai` | Candidate semantics remediation | `CandidateCollectionCell` was made an explicit accessibility element and given the `.button` trait. No visual, candidate-selection, input-state or RIME behavior was changed. A source-contract regression test was added because the Keyboard Extension cannot be linked as a normal XCTest host. |
| `2026-08-22 Asia/Shanghai` | Focused automated verification | `swift-format lint --strict` passed for both changed Swift files. `CandidateAccessibilityContractTests.testCandidateCellDeclaresActionableVoiceOverSemantics` passed (`1` test, `0` failures) under Xcode 27 beta with Swift 6 complete concurrency and warnings-as-errors; the same run compiled the Main App and Keyboard Extension successfully. Result bundle: `/private/tmp/universe-keyboard-voiceover-fix/Logs/Test/Test-Universe Keyboard-2026.08.22_10-57-23-+0800.xcresult`. |
| `2026-08-22 Asia/Shanghai` | Reinstall and Human VoiceOver revalidation | The rebuilt Debug App/Extension was installed on primary Simulator `83BCF163-…`. After cycling away from and back to Universe Keyboard to reload the Extension, the Human operator entered synthetic `ni`, hovered candidate `呢` without clicking, and reported the complete speech “呢，按钮，双击选择候选词”. The original candidate-role residual is closed at Simulator smoke grade. |
| `2026-08-22 Asia/Shanghai` | Full Access-off Chinese → English switch | After disabling VoiceOver, the Human operator cleared the active synthetic `ni` composition, tapped the bottom-row current-language key `中`, observed the key change to `英` and the space label change to `English`, then entered synthetic `abc`. The host received `abc` directly and the candidate bar remained empty, confirming the English path did not incorrectly enter RIME composition. |
| `2026-08-22 Asia/Shanghai` | Full Access-off English → Chinese recovery | The Human operator deleted synthetic `abc`, tapped the bottom-row `英` key, observed restoration of `中` and the `拼音` space label, entered synthetic `ni`, and selected first candidate `你`. The host committed `你` and the marked-text underline cleared normally. This closes the 26-key Chinese/English round-trip smoke. |
| `2026-08-22 Asia/Shanghai` | T9 environment entitlement audit | `simctl get_app_container … groups` returned no shared group, a bounded scan found no `group.com.DoubleShy0N.Universe-Keyboard` container, and `codesign -d --entitlements` printed no entitlements for either installed App or Extension. This matches the current preflight build's `CODE_SIGNING_ALLOWED=NO` setting and the App's visible “共享数据不可用” state. Full Access alone cannot make this unsigned artifact a valid App Group/T9 test environment; a Simulator-signed Debug rebuild retaining entitlements is required before T9 validation. |
| `2026-08-22 Asia/Shanghai` | Simulator-signed entitlement rebuild | A separate Debug build at `/private/tmp/universe-keyboard-ipad18-entitled` succeeded with local “Sign to Run Locally”, Swift 6 complete concurrency and warnings-as-errors. Both generated `*-Simulated.xcent` files contain application identifiers under team `C33N6HTS9N` and the identical `group.com.DoubleShy0N.Universe-Keyboard` entitlement. After install and a clean Main App relaunch, `simctl get_app_container … groups` resolved the expected shared container. No Archive, distribution signing or RC claim is made. |
| `2026-08-22 Asia/Shanghai` | Base RIME shared deployment | Human operator opened Settings → RIME 方案设置 and reported “应用并重新部署” completed successfully. A read-only container inspection confirms deployed `luna_pinyin` shared/build artifacts and user configuration. No `t9.schema.yaml`, `rime_ice.schema.yaml` or T9 readiness artifact is present, so this proves only the base Main-App-owned deployment path; nine-key remains unavailable until the separate fog T9 install/verification flow completes. |
| `2026-08-22 Asia/Shanghai` | Fog T9 install and readiness | Human operator selected 九宫格拼音, personally accepted the presented GPL-3.0 installation gate and reported “九宫格已启用”. Read-only App Group inspection confirms `rime_ice.schema.yaml`, compatible `t9.schema.yaml`, `t9_preedit.lua`, `t9.prism.bin` and fog build/user artifacts. Shared preferences resolve `keyboard_layout_style=nine_key`, `keyboard_layout_scheme_9=t9`, `rime_t9_ready=true`, compatibility version `2`, upstream version `nightly`, and Lua smoke passed. The marker fingerprint `53524f80915dfd8ddef4e9eeba6c01eea2d59a71917f54a695b60089ca4de56f` exactly matches SHA-256 of the deployed shared `t9.schema.yaml`. Main-App-owned T9 preparation is passed; Extension runtime use still requires the Human Full Access permission gate. |
| `2026-08-22 Asia/Shanghai` | Full Access and nine-key Extension reload | Human operator enabled “允许完全访问” in iPadOS Settings and returned to the App. Main App visibly reports 九宫格拼音 / 雾凇拼音 / 资源已就绪. After opening the Search host field and selecting Universe Keyboard without entering text, the Human operator confirmed the Extension rendered grouped nine-key chrome (`ABC` through `WXYZ`). This is runtime evidence that the Extension can read and resolve the verified shared T9 state; actual T9 composition and commit remain pending. |
| `2026-08-22 Asia/Shanghai` | T9 `64` composition and raw-digit boundary | Human operator tapped `MNO` then `GHI`, whose internal T9 identities are `6` and `4`. The host showed underlined readable preedit `ni`; the path bar showed `ni / mi / o / m / n`; initial candidates were `你 / 密 / 米 / 迷 / 秘`. Raw digits `64` were never exposed to the host. This passes T9 key identity → readable provisional composition/path/candidate presentation at Simulator smoke grade; commit and cleanup remain pending. |
| `2026-08-22 Asia/Shanghai` | T9 candidate commit and delete cleanup | Human operator selected first candidate `你` and confirmed the host briefly showed committed `你` without underline while the composition/path state cleared. Only after one subsequent delete did the host, path bar and candidate bar all become empty. This passes the T9 composition → candidate commit → state cleanup → host deletion loop and rules out mistaking a failed selection for a successful delete result. |
| `2026-08-22 Asia/Shanghai` | Nine-key preference Chinese/English round-trip | From idle Chinese nine-key, the Human operator tapped `中`; the Extension changed to full 26-key English chrome, the language key became `英`, the space label became `English`, and synthetic `abc` committed directly with an empty candidate bar. After deleting `abc` and tapping `英`, the Extension returned to Chinese nine-key rather than Chinese 26-key. All transitions matched the explicit product contract that English remains full keyboard under a nine-key preference. Portrait evidence: `/private/tmp/universe-keyboard-ipad18-t9-portrait-ready.png`. |
| `2026-08-22 Asia/Shanghai` | Nine-key portrait → landscape geometry | Human operator rotated only through the Simulator Device menu, without Command shortcuts. Screenshot `/private/tmp/universe-keyboard-ipad18-t9-landscape-ready.png` shows all five nine-key columns, the right delete/kaomoji/tall-return column, bottom functions and safe area without clipping, overlap or stale portrait constraints. The upper undo/redo/paste surface remains the separate iPadOS input-assistant bar. |
| `2026-08-22 Asia/Shanghai` | Landscape T9 touch/composition/commit | Human operator tapped landscape `MNO` then `GHI`; before selection the host showed underlined `ni`, path options and candidates including `你`, with no raw `64`. Selecting `你` produced committed, non-underlined host text and cleared path/candidate UI. Screenshot `/private/tmp/universe-keyboard-ipad18-t9-landscape-committed.png` records the final clean committed state. This passes landscape hit mapping and the T9 presentation/commit loop on the primary Simulator. |
| `2026-08-22 Asia/Shanghai` | Nine-key landscape → portrait recovery | Human operator returned through the Simulator Device menu without Command shortcuts and reported no abnormality. Screenshot `/private/tmp/universe-keyboard-ipad18-t9-returned-portrait.png` shows preserved committed `你`, complete Chinese nine-key chrome and no residual path/candidate state, clipping, overlap or landscape-width constraint. The primary-device nine-key rotation loop is passed. |
| `2026-08-22 Asia/Shanghai` | Small-boundary actual-instance binding | Human operator booted an existing iPad mini (6th generation) / iOS 18.0 and reported UDID `FF9A7369-…`. `simctl list devices --json` confirms that exact instance is Booted and the originally inventoried `D2381C21-…` is a separate unused Shutdown instance of the same model/runtime. The frozen matrix is rebound to the actual instance rather than fabricating continuity. |
| `2026-08-22 Asia/Shanghai` | iPad mini first-run and activation layout | The same Simulator-signed Debug build was installed and launched on `FF9A7369-…`. Screenshots `/private/tmp/universe-keyboard-ipadmini18-main-app-portrait.png` and `/private/tmp/universe-keyboard-ipadmini18-keyboard-added.png` show the Home/welcome and post-add activation guide on the small portrait boundary. Primary actions, explanatory content and checklist remain reachable; no horizontal clipping was observed. Human operator added Universe Keyboard and kept Full Access off; the misleading Full Access affirmation was not used. |
| `2026-08-22 Asia/Shanghai` | iPad mini portrait 26-key geometry/input | Human operator opened the App's Search field and reported the Full Access-off 26-key Extension complete and unobstructed. Screenshot `/private/tmp/universe-keyboard-ipadmini18-keyboard-portrait.png` confirms all key rows, candidate surface, functions and safe area are visible. Human then entered synthetic `ni`, observed underlined composition and candidates, selected first candidate `你`, observed committed text with cleared underline, and deleted once to an empty host/candidate state. Portrait small-boundary geometry and minimal input loop pass. |
| `2026-08-22 Asia/Shanghai` | iPad mini landscape 26-key geometry/input | Human operator rotated through the Simulator Device menu without Command shortcuts. Screenshot `/private/tmp/universe-keyboard-ipadmini18-keyboard-landscape.png` shows complete letter/function rows and safe area without clipping or overlap; the upper undo/redo/paste and prediction row is the system input assistant. Human entered synthetic `ni`, observed underlined composition and Universe candidates without overlap, selected first candidate `你`, and reported stable committed geometry. Screenshot `/private/tmp/universe-keyboard-ipadmini18-landscape-committed.png` records the final state. Small-boundary portrait/landscape critical smoke passes. |
| `2026-08-22 Asia/Shanghai` | Large-boundary actual-instance binding | Human operator booted UDID `CBF6B78C-…`. `simctl` confirms the instance is Booted `iPad Pro 13-inch (M4) (16GB)` on runtime `18.0`; the originally inventoried `1ABCAEE1-…` is a separate unused Shutdown 8GB variant with the same 13-inch screen boundary. The matrix records the actual device rather than treating the variants as the same instance. |
| `2026-08-22 Asia/Shanghai` | 13-inch first-run and activation layout | The same Simulator-signed Debug build was installed and launched on `CBF6B78C-…`. Screenshots `/private/tmp/universe-keyboard-ipadpro13-18-main-app-portrait.png` and `/private/tmp/universe-keyboard-ipadpro13-18-keyboard-added.png` show centered welcome content and the post-add activation guide on the large portrait boundary. Content remains readable/reachable and primary controls preserve usable proportions without uncontrolled full-width stretching. Human added Universe Keyboard with Full Access off; the misleading affirmation was not used. |
| `2026-08-22 Asia/Shanghai` | 13-inch portrait 26-key geometry/input | Human operator loaded Universe Keyboard in the App Search field and reported all 26-key rows, functions, candidate surface and safe area complete. Screenshot `/private/tmp/universe-keyboard-ipadpro13-18-keyboard-portrait.png` confirms the widened keys preserve consistent rows and functional-key proportions without misalignment. Synthetic `ni` produced underlined composition/candidates, first candidate `你` committed cleanly, and one delete returned host/candidate state to empty. Large-boundary portrait critical smoke passes. |
| `2026-08-22 Asia/Shanghai` | 13-inch landscape 26-key geometry/input | Human operator rotated through the Simulator Device menu without Command shortcuts. Screenshot `/private/tmp/universe-keyboard-ipadpro13-18-keyboard-landscape.png` shows the maximum-width 26-key grid, functions, candidate/system-assistant separation and safe area without broken rows, overlap or uncontrolled stretching. Synthetic `ni` produced underlined composition and Universe candidates; selecting first candidate `你` committed cleanly with stable geometry. Screenshot `/private/tmp/universe-keyboard-ipadpro13-18-landscape-committed.png` records the final state. Large-boundary landscape critical smoke passes. |
| `2026-08-22 Asia/Shanghai` | Full local validation audit | Stable Xcode 26.6 passed all `1030` KeyboardCore tests. Xcode 27 passed RimeBridge on iOS 26.0, the complete Main App/Keyboard suites on a separate iPhone 16 / iOS 18.0 Simulator, and strict Debug/Release iOS 18 builds. Exact Xcode 26.6 iOS execution/build remains blocked only by its missing iOS 26.5 Simulator platform/runtime component; the failed destination resolutions and the Xcode 27/iOS 26 allocator instability are preserved as environment evidence rather than converted into product claims. |
| `2026-08-22 Asia/Shanghai` | Full Access deferral remediation and Human revalidation | The App was rebuilt, installed and launched on a separate iPhone 16 Pro / iOS 18.0 Simulator (`2426D713-…`) without erasing the active iPad evidence devices. Runtime accessibility inspection exposed “稍后再开启” as an actionable button. The Human operator tapped it and confirmed that the App entered resource preparation/scheme selection rather than navigating to Search. Automated state tests separately prove that Full Access remains incomplete, returns after J3/J4, and an observed shared-data failure overrides deferral. This closes the identified activation affordance residual at Simulator smoke grade only. |

## Conclusion

The primary iPad Simulator critical path has passed 26-key and nine-key portrait/landscape rotation, light/dark appearance, AXXXL reachability, Chinese/English layout round-trips, T9 raw-digit isolation/commit/delete and the remediated candidate/expand-control VoiceOver smoke. Actual iPad mini 6 and actual 13-inch M4 iOS 18 instances also passed Full Access-off portrait/landscape 26-key geometry and minimal input/commit smoke. Automated iOS 18 test/build evidence is green, while the exact local Xcode 26.6 iOS gate still requires its iOS 26.5 Simulator platform/runtime component (or equivalent Xcode Cloud execution). Diagnostics breadth and complete VoiceOver navigation/order remain pending. This evidence does not approve RC freeze, physical-device coverage, Archive/signing, TestFlight upload or the Product Gate.
